# Template only — create real Secret via Azure Key Vault CSI or kubectl create secret generic.
# kubectl create secret generic opnbnk4-db -n opnbnk4 \
#   --from-literal=url=jdbc:postgresql://... \
#   --from-literal=username=bank \
#   --from-literal=password=...
apiVersion: v1
kind: Secret
metadata:
  name: opnbnk4-db
  namespace: opnbnk4
type: Opaque
stringData:
  url: "jdbc:postgresql://REPLACE:5432/bank"
  username: "REPLACE"
  password: "REPLACE"
