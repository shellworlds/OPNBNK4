# OpenAPI exports (placeholder)

Run `springdoc-openapi` (or equivalent) on each Spring service and save:

- `account-service.json`
- `transaction-service.json`
- `openbanking-service.json`
- `api-gateway.json`

Commit here or attach to the client package. Until then, use `docs/architecture/api-reference.md` and E2E tests as the contract baseline.
