server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: account-service
          uri: ${GATEWAY_ACCOUNT_URI:http://account-service:8081}
          predicates:
            - Path=/api/accounts/**
        - id: transaction-service
          uri: ${GATEWAY_TRANSACTION_URI:http://transaction-service:8082}
          predicates:
            - Path=/api/transactions/**
        - id: openbanking-service
          uri: ${GATEWAY_OPENBANKING_URI:http://openbanking-service:8083}
          predicates:
            - Path=/api/openbanking/**

management:
  endpoints:
    web:
      exposure:
        include: health,info
