# Mobile app (stub)

This folder is reserved for a **React Native** client. Day 1 does not scaffold the full native toolchain; when you are ready, initialize the app here (e.g. React Native CLI or Expo) and point API calls at the same gateway base URL pattern as the web portal.
