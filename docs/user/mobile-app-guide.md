# Mobile application guide

- **Expo:** from `frontend/mobile-app`, follow README for `npx expo start`.
- **Static web (Compose):** `mobile-web` service on port **3010** for quick demos.
- Point `EXPO_PUBLIC_GATEWAY_URL` (or build args) at the correct gateway.

**Offline:** show last cached balances when implemented; currently network-required for fresh data.
